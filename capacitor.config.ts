import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cristianlagos.hospitalfamilia',
  appName: 'Hospital - Familia',
  webDir: 'www'
};

export default config;
